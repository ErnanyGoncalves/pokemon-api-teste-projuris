export class Pokedex {
    public id: number;
    public name: string;
    public sprite: string;

    constructor(id: number, name: string, sprite: string) {
        this.id = id;
        this.name = name;
        this.sprite = sprite;
    }

}